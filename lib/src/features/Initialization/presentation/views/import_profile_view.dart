import 'package:flutter/material.dart';

class ImportProfileView extends StatelessWidget {
  const ImportProfileView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
